# End-to-end tests

No complete Windows MVP E2E scenario exists at Milestone 1. The desktop shell smoke
and GUI integration tests live in `tests/integration`; they do not verify automation.
Do not count this directory as a passing Windows test. Add executable tests as needed.
The full Windows acceptance sequence and opt-in execution rules are in
`docs/TESTING.md`.
